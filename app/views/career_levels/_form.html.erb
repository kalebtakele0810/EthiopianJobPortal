<%= form_with(model: career_level) do |form| %>
  <% if career_level.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(career_level.errors.count, "error") %> prohibited this career_level from being saved:</h2>

      <ul>
        <% career_level.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= form.label :name %>
    <%= form.text_field :name, class:"form-control" %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
