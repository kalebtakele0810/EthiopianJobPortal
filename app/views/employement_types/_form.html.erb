<%= form_with(model: employement_type) do |form| %>
  <% if employement_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(employement_type.errors.count, "error") %> prohibited this employement_type from being saved:</h2>

      <ul>
        <% employement_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
